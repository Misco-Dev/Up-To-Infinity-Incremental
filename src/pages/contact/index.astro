---
import Layout from "@/layouts/Layout.astro";
---

<Layout title="Up To Infinity Incremental | Contact">
    <main role="main">
        <div class="contact-container">
            <div class="contact-card">
                <h1 class="title">Contact us</h1>
                <p class="subtitle">Join our community and stay updated</p>
                
                <div class="social-links">
                    <a href="https://discord.gg/6pzc7Dny" target="_blank" class="social-link discord">
                        <span class="social-icon">📱</span>
                        <span class="social-name">Discord</span>
                        <span class="social-description">Join our community</span>
                    </a>
                    
                    <a href="/" target="_blank" class="social-link twitter">
                        <span class="social-icon">🐦</span>
                        <span class="social-name">Twitter</span>
                        <span class="social-description">Follow for updates</span>
                    </a>
                </div>

                <div class="contact-info">
                    <h2>Contact Information</h2>
                    <p>For business inquiries or support:</p>
                    <a href="mailto:contact@qronexstudio.com" class="email-link">
                        contact@qronexstudio.com
                    </a>
                </div>
            </div>
        </div>
    </main>
</Layout>

<style>
    .contact-container {
        min-height: 80vh;
        display: grid;
        place-items: center;
        padding: 2rem;
    }

    .contact-card {
        width: 100%;
        max-width: 800px;
        padding: 3rem;
        background: rgba(0, 0, 0, 0.5);
        border-radius: 24px;
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        text-align: center;
    }

    .title {
        font-size: clamp(2rem, 5vw, 3rem);
        margin-bottom: 0.5rem;
        background: linear-gradient(135deg, #8a5be2, #ff69b4);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .subtitle {
        font-size: 1.2rem;
        color: #cdcdcd;
        margin-bottom: 3rem;
    }

    .social-links {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
        margin-bottom: 3rem;
    }

    .social-link {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 2rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 16px;
        text-decoration: none;
        transition: transform 0.3s ease, background-color 0.3s ease;
    }

    .social-link:hover {
        transform: translateY(-5px);
        background: rgba(255, 255, 255, 0.1);
    }

    .social-icon {
        font-size: 2.5rem;
        margin-bottom: 1rem;
    }

    .social-name {
        font-size: 1.25rem;
        color: #fff;
        margin-bottom: 0.5rem;
        font-weight: bold;
    }

    .social-description {
        color: #cdcdcd;
        font-size: 0.9rem;
    }

    .contact-info {
        margin-top: 3rem;
        padding-top: 2rem;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
    }

    .contact-info h2 {
        font-size: 1.5rem;
        color: #fff;
        margin-bottom: 1rem;
    }

    .contact-info p {
        color: #cdcdcd;
        margin-bottom: 1rem;
    }

    .email-link {
        color: #8a5be2;
        text-decoration: none;
        border-bottom: 2px solid transparent;
        transition: border-color 0.3s ease;
        font-weight: bold;
    }

    .email-link:hover {
        border-color: #8a5be2;
    }

    @media (max-width: 768px) {
        .contact-container {
            padding: 1rem;
            min-height: 70vh;
        }

        .contact-card {
            padding: 2rem 1.25rem;
            border-radius: 16px;
            margin: 1rem;
        }

        .title {
            font-size: 2rem;
            margin-bottom: 0.75rem;
        }

        .subtitle {
            font-size: 1rem;
            margin-bottom: 2rem;
        }

        .social-links {
            grid-template-columns: 1fr;
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .social-link {
            padding: 1.5rem;
            border-radius: 12px;
        }

        .social-icon {
            font-size: 2rem;
            margin-bottom: 0.75rem;
        }

        .social-name {
            font-size: 1.1rem;
            margin-bottom: 0.35rem;
        }

        .social-description {
            font-size: 0.85rem;
        }

        .contact-info {
            margin-top: 2rem;
            padding-top: 1.5rem;
        }

        .contact-info h2 {
            font-size: 1.25rem;
            margin-bottom: 0.75rem;
        }

        .contact-info p {
            font-size: 0.9rem;
            margin-bottom: 0.75rem;
        }

        .social-link:hover {
            transform: none;
        }
    }

    @media (max-width: 480px) {
        .contact-container {
            padding: 0.75rem;
            min-height: 65vh;
        }

        .contact-card {
            padding: 1.5rem 1rem;
            border-radius: 12px;
            margin: 0.5rem;
        }

        .title {
            font-size: 1.75rem;
        }

        .subtitle {
            font-size: 0.95rem;
            margin-bottom: 1.5rem;
        }

        .social-links {
            gap: 0.75rem;
        }

        .social-link {
            padding: 1.25rem;
            border-radius: 10px;
        }

        .social-icon {
            font-size: 1.75rem;
            margin-bottom: 0.5rem;
        }

        .social-name {
            font-size: 1rem;
        }

        .social-description {
            font-size: 0.8rem;
        }

        .contact-info {
            margin-top: 1.5rem;
            padding-top: 1.25rem;
        }

        .contact-info h2 {
            font-size: 1.15rem;
        }

        .contact-info p {
            font-size: 0.85rem;
        }

        .email-link {
            font-size: 0.9rem;
            word-break: break-all;
        }
    }
</style>


