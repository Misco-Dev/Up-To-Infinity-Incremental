
---
import Layout from "@/layouts/Layout.astro"
import Countdown from "@/components/countdown.astro"
---

<Layout title="Up To Infinity Incremental">
    <main role="main">
        <div class="hero-section">
            <div class="container" role="textbox" aria-label="Introducing content">
                <h1 class="title" role="heading">Up To Infinity Incremental</h1>
                <p class="introduction" role="complementary">Welcome to the Future of Incremental Games</p>
                <p class="introduction-content" role="contentinfo">How this game works?</p>
                <p role="definition" class="introduction-content-description">
                    This game works by getting infinities, which you can later use for better upgrades to get better at the game
                </p>
                <div class="features">
                    <div class="feature-item">
                        <span class="feature-icon">∞</span>
                        <h4>Infinite Possibilities</h4>
                        <p>Unlock endless upgrades and achievements</p>
                    </div>
                    <div class="feature-item">
                        <span class="feature-icon">⚡</span>
                        <h4>Fast Progress</h4>
                        <p>Quick and satisfying progression system</p>
                    </div>
                    <div class="feature-item">
                        <span class="feature-icon">🎮</span>
                        <h4>Dynamic Gameplay</h4>
                        <p>Engaging mechanics that keep you coming back</p>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="countdown-section">
            <h3 class="countdown-text">The game will be released in</h3>
            <Countdown />
        </div>
    </main>
</Layout>

<style>
    @keyframes fadeInUp {
        0% {
            opacity: 0;
            transform: translateY(20px);
        }
        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .hero-section {
        min-height: 80vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
        animation: fadeInUp 1s ease forwards;
        animation-delay: 0.2s;
        opacity: 0;
    }

    .container {
        max-width: 1100px;
        margin: 0 auto;
        padding: 3rem;
        text-align: center;
        background: #ffffff0d;
        border-radius: 24px;
        box-shadow: 0 8px 32px #0000004d;
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        border: 1px solid #ffffff1a;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        animation: fadeInUp 1s ease forwards;
        animation-delay: 0.4s;
        opacity: 0;
    }

    .container:hover {
        transform: translateY(-5px);
		backdrop-filter: blur(0);
        box-shadow: 0 12px 48px rgba(0, 0, 0, 0.4);
    }

    .title {
        font-size: clamp(2.5rem, 5vw, 4rem);
        font-weight: 800;
        background: linear-gradient(135deg, #8a5be2, #ff69b4);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 1.5rem;
        text-shadow: none;
        animation: fadeInUp 1s ease forwards;
        animation-delay: 0.6s;
        opacity: 0;
    }

    .introduction {
        font-size: clamp(1.5rem, 3vw, 2.5rem);
        color: #fff;
        font-weight: bold;
        text-transform: uppercase;
        margin: 1.5rem 0;
        letter-spacing: 0.1em;
        animation: fadeInUp 1s ease forwards;
        animation-delay: 0.8s;
        opacity: 0;
    }

    .introduction-content {
        font-size: clamp(1.25rem, 2.5vw, 2rem);
        color: #eee;
        margin: 1rem 0;
    }

    .introduction-content-description {
        font-size: clamp(1rem, 2vw, 1.25rem);
        color: #ddd;
        margin: 1rem auto;
        max-width: 800px;
        line-height: 1.6;
    }

    .features {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
        margin-top: 3rem;
        padding: 1rem;
        animation: fadeInUp 1s ease forwards;
        animation-delay: 1s;
        opacity: 0;
    }

    .feature-item {
        padding: 1.5rem;
        background: #8a5be21a;
        border-radius: 16px;
        transition: transform 0.3s ease;
    }

    .feature-item:hover {
        transform: translateY(-5px);
    }

    .feature-icon {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        display: inline-block;
    }

    .feature-item h4 {
        font-size: 1.25rem;
        color: #fff;
        margin: 0.5rem 0;
    }

    .feature-item p {
        color: #ddd;
        font-size: 1rem;
        line-height: 1.4;
    }

    .countdown-section {
        text-align: center;
        padding: 3rem 1rem;
    }

    .countdown-text {
        font-size: clamp(1.5rem, 3vw, 2rem);
        color: #fff;
        margin-bottom: 2rem;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        background: linear-gradient(135deg, #8a5be2, #ff69b4);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        animation: fadeInUp 1s ease forwards;
        animation-delay: 1.2s;
        opacity: 0;
    }

    @media (max-width: 768px) {
        .hero-section {
            padding: 1.25rem;
            min-height: auto;
        }

        .container {
            padding: 2rem 1.25rem;
            margin: 1.25rem;
            border-radius: 18px;
        }

        .title {
            font-size: 2.75rem;
            margin-bottom: 1.25rem;
        }

        .introduction {
            font-size: 1.75rem;
            margin: 1.25rem 0;
        }

        .introduction-content {
            font-size: 1.35rem;
        }

        .introduction-content-description {
            font-size: 1.1rem;
            margin: 1rem auto;
            padding: 0 0.75rem;
        }

        .features {
            grid-template-columns: 1fr;
            gap: 1.5rem;
            margin-top: 2.5rem;
            padding: 0.75rem;
        }

        .feature-item {
            padding: 1.5rem;
            border-radius: 14px;
        }

        .feature-icon {
            font-size: 2.25rem;
            margin-bottom: 1rem;
        }

        .feature-item h4 {
            font-size: 1.25rem;
            margin: 0.5rem 0;
        }

        .feature-item p {
            font-size: 1rem;
        }

        .countdown-section {
            padding: 2.5rem 1.25rem;
        }

        .countdown-text {
            font-size: 1.75rem;
            margin-bottom: 1.75rem;
        }
    }

    @media (max-width: 480px) {
        .hero-section {
            padding: 1rem;
        }

        .container {
            padding: 1.5rem 1rem;
            margin: 0.75rem;
            border-radius: 14px;
        }

        .title {
            font-size: 2.25rem;
        }

        .introduction {
            font-size: 1.5rem;
            margin: 1rem 0;
        }

        .introduction-content {
            font-size: 1.25rem;
        }

        .introduction-content-description {
            font-size: 1.1rem;
            line-height: 1.6;
            padding: 0 0.5rem;
        }

        .features {
            gap: 1.25rem;
            margin-top: 1.75rem;
            padding: 0.5rem;
        }

        .feature-item {
            padding: 1.25rem;
            border-radius: 12px;
        }

        .feature-icon {
            font-size: 2rem;
            margin-bottom: 0.75rem;
        }

        .feature-item h4 {
            font-size: 1.1rem;
        }

        .feature-item p {
            font-size: 1rem;
            line-height: 1.5;
        }

        .countdown-section {
            padding: 2rem 1rem;
        }

        .countdown-text {
            font-size: 1.5rem;
            margin-bottom: 1.25rem;
        }

        .container:hover {
            transform: none;
            box-shadow: 0 8px 32px #0000004d;
        }

        .feature-item:hover {
            transform: none;
        }
    }
</style>
