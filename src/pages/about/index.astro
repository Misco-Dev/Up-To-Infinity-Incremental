---
import Layout from "@/layouts/Layout.astro";
---

<Layout title="About Us | Up To Infinity Incremental">
    <main role="main">
        <div class="about-container">
            <section class="hero-section">
                <h1 class="title">Qronex Studio</h1>
                <p class="subtitle">Creators of Up To Infinity Incremental</p>
            </section>

            <section class="content-section">
                <div class="card about-card-1">
                    <h2 class="section-title">About The Game</h2>
                    <p class="section-text">
                        Up To Infinity Incremental is a unique incremental game that challenges players to reach infinity through strategic upgrades and careful resource management.
                    </p>
                </div>

                <div class="card about-card-2">
                    <h2 class="section-title">Our Story</h2>
                    <p class="section-text">
                        Created by <a href="http://qronexstudiosai.work.gd" target="_blank" class="company-link">Qronex Studio</a>, 
                        we're passionate about creating engaging incremental games that challenge and entertain players.
                    </p>
                </div>

                <div class="features-grid">
                    <div class="feature-card feature-card-1">
                        <span class="feature-icon">∞</span>
                        <h3>Infinite Possibilities</h3>
                        <p>Endless upgrade paths and achievements to unlock</p>
                    </div>
                    <div class="feature-card feature-card-2">
                        <span class="feature-icon">⚡</span>
                        <h3>Fast Progress</h3>
                        <p>Quick and satisfying progression system</p>
                    </div>
                    <div class="feature-card feature-card-3">
                        <span class="feature-icon">🎮</span>
                        <h3>Dynamic Gameplay</h3>
                        <p>Engaging mechanics that keep you coming back</p>
                    </div>
                </div>
            </section>
        </div>
    </main>
</Layout>

<style>
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .about-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
        animation: fadeInUp 0.7s ease forwards;
        animation-delay: 0.1s;
        opacity: 0;
    }

    .hero-section {
        text-align: center;
        margin-bottom: 4rem;
        padding: 4rem 2rem;
        background: #0005;
        border-radius: 1rem;
        backdrop-filter: blur(10px);
        border: 1px solid #ffffff17;
        animation: fadeInUp 0.7s ease forwards;
        animation-delay: 0.3s;
        opacity: 0;
    }

    .title {
        font-size: clamp(2.5rem, 5vw, 4rem);
        font-weight: 800;
        background: linear-gradient(135deg, #8a5be2, #ff69b4);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 1rem;
        animation: fadeInUp 0.7s ease forwards;
        animation-delay: 0.5s;
        opacity: 0;
    }

    .subtitle {
        font-size: clamp(1.25rem, 2.5vw, 1.5rem);
        color: inherit;
        opacity: 0.9;
        animation: fadeInUp 0.7s ease forwards;
        animation-delay: 0.7s;
        opacity: 0;
    }

    .content-section {
        display: grid;
        gap: 2rem;
        animation: fadeInUp 0.7s ease forwards;
        animation-delay: 0.9s;
        opacity: 0;
    }

    .card {
        background: #ffffff09;
        padding: 2rem;
        border-radius: 1rem;
        backdrop-filter: blur(10px);
        border: 1px solid #ffffff17;
        transition: transform 0.3s ease;
        opacity: 0;
        animation: fadeInUp 0.7s ease forwards;
    }

    .about-card-1 {
        animation-delay: 1.1s;
    }

    .about-card-2 {
        animation-delay: 1.3s;
    }

    .card:hover {
        transform: translateY(-5px);
    }

    .section-title {
        font-size: 1.5rem;
        color: #fff;
        margin-bottom: 1rem;
    }

    .section-text {
        color: #cdcdcd;
        line-height: 1.6;
    }

    .company-link {
        color: #8a5be2;
        text-decoration: none;
        border-bottom: 2px solid transparent;
        transition: border-color 0.3s ease;
    }

    .company-link:hover {
        border-color: #8a5be2;
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
        margin-top: 2rem;
        animation: fadeInUp 0.7s ease forwards;
        animation-delay: 1.5s;
        opacity: 0;
    }

    .feature-card {
        background: #8a5be21a;
        padding: 2rem;
        border-radius: 1rem;
        text-align: center;
        transition: transform 0.3s ease;
        opacity: 0;
        animation: fadeInUp 0.7s ease forwards;
    }

    .feature-card-1 {
        animation-delay: 1.7s;
    }

    .feature-card-2 {
        animation-delay: 1.9s;
    }

    .feature-card-3 {
        animation-delay: 2.1s;
    }

    .feature-card:hover {
        transform: translateY(-5px);
    }

    .feature-icon {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        display: inline-block;
    }

    .feature-card h3 {
        color: #fff;
        margin: 1rem 0;
        font-size: 1.25rem;
    }

    .feature-card p {
        color: #cdcdcd;
        font-size: 1rem;
        line-height: 1.4;
    }

    .about-container {
        cursor: default;
    }
</style>
