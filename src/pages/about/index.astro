---
import Layout from "@/layouts/Layout.astro";
---

<Layout title="Up To Infinity Incremental | About Us">
    <main role="main" class="about-page-main">
        <div class="main-container">
            <section class="hero-section" aria-labelledby="studio-title">
                <h1 id="studio-title" class="title" role="heading" aria-level="1">Qronex Studio</h1>
                <p class="subtitle">Creators of Up To Infinity Incremental</p>
            </section>

            <section class="content-section">
                <div class="card about-card-1" role="article" aria-labelledby="about-game-title">
                    <h2 id="about-game-title" class="section-title" role="heading" aria-level="2">About The Game</h2>
                    <p class="section-text">
                        Up To Infinity Incremental is a unique incremental game that challenges players to reach infinity through strategic upgrades and careful resource management.
                    </p>
                </div>

                <div class="card about-card-2" role="article" aria-labelledby="our-story-title">
                    <h2 id="our-story-title" class="section-title" role="heading" aria-level="2">Our Story</h2>
                    <p class="section-text">
                        Created by <a href="http://qronexstudiosai.work.gd" target="_blank" rel="noopener noreferrer" class="company-link" aria-label="Visitar sitio web de Qronex Studio">Qronex Studio</a>, we're passionate about creating engaging incremental games that challenge and entertain players.
                    </p>
                </div>

                <div class="features-grid" aria-label="Características clave del juego">
                    <div class="feature-card feature-card-1" role="listitem">
                        <span class="feature-icon" aria-hidden="true">∞</span>
                        <h2>Infinite Possibilities</h2>
                        <p>Endless upgrade paths and achievements to unlock.</p>
                    </div>
                    <div class="feature-card feature-card-2" role="listitem">
                        <span class="feature-icon" aria-hidden="true">⚡</span>
                        <h2>Fast Progress</h2>
                        <p>Quick and satisfying progression system.</p>
                    </div>
                    <div class="feature-card feature-card-3" role="listitem">
                        <span class="feature-icon" aria-hidden="true">🎮</span>
                        <h2>Dynamic Gameplay</h2>
                        <p>Engaging mechanics that keep you coming back.</p>
                    </div>
                </div>
            </section>
        </div>
    </main>
</Layout>

<style>
    :root {
        --color-primary: #8a5be2;
        --color-secondary: #ff69b4;
        --color-text-light: #fff;
        --color-text-medium: #eee;
        --color-text-dark: #ddd;
        --color-bg-transparent-light: #ffffff0d;
        --color-bg-transparent-medium: #ffffff1a;
        --color-shadow-light: #00000033;
        --color-shadow-medium: #0000004d;
    }

    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .about-page-main {
        max-width: 100%;
        padding: 2rem;
    }

    .main-container {
        max-width: 1200px;
        margin: 0 auto;
        animation: fadeInUp 0.8s ease forwards;
        animation-delay: 0.1s;
        opacity: 0;
        cursor: default;
    }

    .hero-section {
        text-align: center;
        margin-bottom: 4rem;
        padding: 4rem 2rem;
        background: #00000066;
        border-radius: 1.5rem;
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid #ffffff26;
        box-shadow: 0 8px 30px #00000066;
        animation: fadeInUp 0.8s ease forwards;
        animation-delay: 0.3s;
        opacity: 0;
    }

    .title {
        font-size: clamp(2.5rem, 5vw, 4rem);
        font-weight: 800;
        background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 1rem;
        text-shadow: 0 4px 15px #8a5be266;
        animation: fadeInUp 0.8s ease forwards;
        animation-delay: 0.5s;
        opacity: 0;
    }

    .subtitle {
        font-size: clamp(1.25rem, 2.5vw, 1.5rem);
        color: var(--color-text-medium);
        opacity: 0.9;
        text-shadow: 0 1px 6px #0000004d;
        animation: fadeInUp 0.8s ease forwards;
        animation-delay: 0.7s;
        opacity: 0;
    }

    .content-section {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2.5rem;
        animation: fadeInUp 0.8s ease forwards;
        animation-delay: 0.9s;
        opacity: 0;
    }

    .card {
        background: #ffffff14;
        padding: 2.5rem;
        border-radius: 1.5rem;
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        border: 1px solid #ffffff26;
        transition: transform 0.3s ease, box-shadow 0.3s ease, background 0.3s ease;
        opacity: 0;
        animation: fadeInUp 0.8s ease forwards;
        box-shadow: 0 6px 20px #00000033;
    }

    .about-card-1 {
        animation-delay: 1.1s;
    }

    .about-card-2 {
        animation-delay: 1.3s;
    }

    .card:hover {
        transform: translateY(-8px);
        box-shadow: 0 12px 30px #00000066; 
        background: #ffffff26;
    }

    .section-title {
        font-size: 1.8rem;
        color: var(--color-text-light);
        margin-bottom: 1.2rem;
        font-weight: 700;
    }

    .section-text {
        color: var(--color-text-dark);
        line-height: 1.7;
        font-size: 1.05rem;
    }

    .company-link {
        color: var(--color-secondary);
        text-decoration: none;
        border-bottom: 2px solid transparent;
        transition: border-color 0.3s ease, color 0.3s ease;
        font-weight: bold;
    }

    .company-link:hover {
        border-color: var(--color-secondary);
        color: #ff8cc0;
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
        margin-top: 3rem;
        opacity: 0;
        animation: fadeInUp 0.8s ease forwards;
        animation-delay: 1.5s;
    }

    .feature-card {
        background: #8a5be226;
        padding: 2.2rem;
        border-radius: 1.2rem;
        text-align: center;
        transition: transform 0.3s ease, box-shadow 0.3s ease, background 0.3s ease;
        opacity: 0;
        animation: fadeInUp 0.8s ease forwards;
        border: 1px solid #ffffff1a;
        box-shadow: 0 4px 15px #00000033;
    }

    .feature-card-1 {
        animation-delay: 1.7s;
    }

    .feature-card-2 {
        animation-delay: 1.9s;
    }

    .feature-card-3 {
        animation-delay: 2.1s;
    }

    .feature-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 12px 30px #00000066;
        background: #8a5be240;
    }

    .feature-icon {
        font-size: 3rem;
        margin-bottom: 1.2rem;
        display: inline-block;
        color: var(--color-secondary);
        text-shadow: 0 0 15px #ff69b4b3;
    }

    .feature-card h3 {
        color: var(--color-text-light);
        margin: 1rem 0;
        font-size: 1.4rem;
    }

    .feature-card p {
        color: var(--color-text-dark);
        font-size: 1.05rem;
        line-height: 1.6;
    }

    @media (max-width: 768px) {
        .hero-section {
            padding: 3rem 1.5rem;
            margin-bottom: 3rem;
        }
        .title {
            font-size: 2.2rem;
        }
        .subtitle {
            font-size: 1.1rem;
        }
        .content-section {
            grid-template-columns: 1fr;
            gap: 2rem;
        }
        .card {
            padding: 2rem;
            border-radius: 1.2rem;
        }
        .section-title {
            font-size: 1.5rem;
        }
        .section-text {
            font-size: 1rem;
        }
        .features-grid {
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }
        .feature-card {
            padding: 1.8rem;
        }
        .feature-icon {
            font-size: 2.5rem;
        }
        .feature-card h3 {
            font-size: 1.25rem;
        }
        .feature-card p {
            font-size: 1rem;
        }
    }

    @media (max-width: 480px) {
        .about-page-main {
            padding: 1rem;
        }
        .hero-section {
            padding: 2.5rem 1rem;
            margin-bottom: 2rem;
        }
        .title {
            font-size: 1.8rem;
        }
        .subtitle {
            font-size: 1rem;
        }
        .card {
            padding: 1.5rem;
            border-radius: 1rem;
        }
        .section-title {
            font-size: 1.3rem;
        }
        .section-text {
            font-size: 0.9rem;
            line-height: 1.5;
        }
        .features-grid {
            grid-template-columns: 1fr;
            gap: 1.2rem;
            margin-top: 1.5rem;
        }
        .feature-card {
            padding: 1.5rem;
            border-radius: 1rem;
        }
        .feature-icon {
            font-size: 2.2rem;
            margin-bottom: 1rem;
        }
        .feature-card h3 {
            font-size: 1.1rem;
            margin: 0.8rem 0;
        }
        .feature-card p {
            font-size: 0.9rem;
            line-height: 1.5;
        }

        .card:hover, .feature-card:hover {
            transform: none;
            box-shadow: initial;
            background: initial;
        }
    }
</style>